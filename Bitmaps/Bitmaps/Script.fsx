open System.Drawing

let bitmap = new Bitmap(16,16)

let path = __SOURCE_DIRECTORY__ + "/"

bitmap.Save(path + "bitmap.png")

open System.Drawing

let bitmap = new Bitmap(16,16)

let path = __SOURCE_DIRECTORY__ + "/"


bitmap.Save(path + "bitmap.png")